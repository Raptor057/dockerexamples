# Usa la imagen base de Debian
FROM debian

RUN apt-get update && apt-get install -y apache2 && apt-get install -y ufw

CMD [ "sudo systemctl start apache2" ]

#RUN echo "Apache2 ha sido instalado con éxito."

#RUN apt-get update && apt-get install -y nodejs

#RUN echo "nodejs ha sido instalado con éxito."

#RUN apt-get update && apt-get install -y git

#RUN echo "git ha sido instalado con éxito."

